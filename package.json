{
  "name": "crabpen",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "wasm:dev": "wasm-pack build wasm --target web --dev",
    "wasm:build": "wasm-pack build wasm --target web",
    "build": "npm run wasm:build && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/p5": "^1.7.7",
    "typescript": "~6.0.2",
    "vite": "^8.0.12",
    "vite-plugin-wasm": "^3.6.0"
  },
  "dependencies": {
    "p5": "^2.2.3"
  }
}